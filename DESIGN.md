# DESIGN.md — KimHands 포트폴리오

> 이 문서가 디자인의 단일 진실 소스(source of truth)다. 색·타이포·간격은 토큰으로만 참조한다.

## 철학

**"페이지는 랜딩이 아니라 문서다."**
Ollama(문서 우선 미니멀리즘)와 Apple(타이포 절제 · 압도적 여백 · 단일 액센트)에서 calibrate.
화면을 잘 조판된 README처럼 다룬다 — 마케팅 히어로가 아니라, 읽히는 문서.

- 모노크롬 베이스 + **액센트 단 하나**(#0066cc)
- 그라데이션 · 드롭섀도 · 글래스모피즘 **없음**. 깊이는 표면색 변화 또는 1px 헤어라인으로만.
- **코드는 1급 요소** — 터미널 카드/모노스페이스가 장식이 아니라 콘텐츠.
- 여백이 곧 레이아웃. 시원하게.
- 시스템 폰트 우선(맥=SF Pro). Inter 남발 자체가 요즘 "AI 티"라 지양.

## 색상 토큰

### 라이트 (기본)
| 토큰 | 값 | 용도 |
|---|---|---|
| `canvas` | `#ffffff` | 페이지 배경 |
| `soft` | `#fafafa` | 보조 표면(코드 스니펫 등) |
| `ink` | `#1d1d1f` | 헤드라인·본문 강조(순흑 대신 near-black) |
| `charcoal` | `#525252` | 본문 |
| `body` | `#737373` | 보조 본문·메타 |
| `mute` | `#a3a3a3` | 캡션·라벨·최저 강조 |
| `hairline` | `#e5e5e5` | 1px 보더·구분선 |
| `hairline-strong` | `#d4d4d4` | 강한 구분선 |
| `dark` | `#171717` | 반전 블록(연락처) — "여기 봐" 모먼트 1회 |
| `on-dark` / `on-dark-mute` | `#ffffff` / `rgba(255,255,255,.66)` | 다크 블록 위 텍스트 |
| **`accent`** | **`#0066cc`** | 유일한 액센트 — 라이브/외부 링크, 핵심 인터랙션, 포커스 |
| `accent-soft` | `#eaf2fc` | 액센트 배경(호버/하이라이트, 드물게) |

### 다크 (토글, 포함)
| 토큰 | 값 | 용도 |
|---|---|---|
| `canvas` | `#0b0b0c` | 페이지 배경 |
| `soft` | `#161618` | 보조 표면 |
| `ink` | `#f5f5f7` | 헤드라인·강조 |
| `charcoal` | `#c7c7cc` | 본문 |
| `body` | `#98989d` | 보조 본문·메타 |
| `mute` | `#6b6b70` | 캡션·라벨 |
| `hairline` | `#2a2a2d` | 1px 보더 |
| `hairline-strong` | `#3a3a3d` | 강한 구분선 |
| `surface-raised` | `#1c1c1e` | 연락 블록(다크에선 반전 = 살짝 밝은 표면) |
| `accent` | `#2997ff` | 다크 배경 대비용 밝은 블루(라이트 `#0066cc` 대응) |

> 토글: `prefers-color-scheme` 초기값 + `localStorage` 저장. 전환 시 색 토큰만 스왑(레이아웃 불변). 한·영 토글과 함께 nav에 배치.

## 타이포그래피

- **Sans**: `-apple-system, BlinkMacSystemFont, system-ui, "Inter", sans-serif` (웹 폴백 Inter)
- **Mono**: `ui-monospace, SFMono-Regular, "SF Mono", Menlo, "JetBrains Mono", monospace`
- **Weight 사다리**: 400 / 500 / 600 (700 남발 금지)
- **본문 17px**, line-height 1.5–1.6, `letter-spacing: -.01em`
- **디스플레이는 음수 자간**(`-.02 ~ -.035em`) — "타이트한" 헤드라인 톤

| 토큰 | 크기 / 두께 / 자간 | 용도 |
|---|---|---|
| `hero` | 60px / 600 / -.035em | 이름(데스크탑) |
| `h2` | 30px / 600 / -.025em | 다크 블록 헤드 |
| `h3` | 21px / 600 / -.02em | 카드 제목 |
| `lead` | 19px / 400 | 히어로 소개 |
| `body` | 17px / 400 | 본문 |
| `small` | 15px / 400 | 카드 본문 |
| `label` (mono) | 12–13px / 대문자 / .06em | 섹션 라벨·메타 |
| `code` (mono) | 14px / 400 | 터미널·기술 태그 |

## 간격 · 레이아웃

- 베이스 8px. 섹션 간격 **88px**(모바일 56–64px).
- 컨테이너 **max 1080px**, 좌우 패딩 40px(모바일 24px).
- 본문 글줄 폭(measure)은 **~640px로 제한** — 넓은 캔버스에서도 가독성 유지.
- 카드 패딩 28px. 히어로는 2단(텍스트 / 터미널 카드), 880px 이하 1단.

## 모양 · 깊이

- **Radius**: 버튼/입력 = `pill`(9999px), 카드/터미널 = `14px`, 구조선 = `0`.
- **Elevation**: 그림자 없음. 1px 헤어라인 보더 또는 `dark` 표면 반전만 허용.

## 모션 (절제 필수)

- 진입: `opacity 0→1` + `translateY(16px→0)`, 700ms `cubic-bezier(.2,.7,.2,1)`, 스태거 ~80ms.
- 터미널: 줄 단위 순차 등장(~210ms 간격) + 마지막 줄 액센트 커서 깜빡임.
- 호버: 네비 밑줄 좌→우 그어짐(scaleX).
- **`prefers-reduced-motion: reduce`면 모든 모션 비활성**.
- 패럴랙스·과한 트랜지션·자동재생 캐러셀 금지.

## 컴포넌트

- **nav**: 좌 이름 / 우 텍스트 링크 + 검정 pill(GitHub). 56–60px, 하단 헤어라인.
- **terminal-card**: 트래픽 라이트 3색 + 모노 본문. 히어로의 유일한 비주얼.
- **project-card**: 1px 헤어라인, radius 14, 그림자 없음. 제목·메타·요약·기술(mono 평문)·라이브/코드 링크. 호버 시 보더만 `ink`로.
- **def-list**: Stack을 칩 클라우드가 아니라 정의 목록(`dt` mono 라벨 / `dd` 값)으로.
- **contact-dark**: 유일한 반전 블록. 흰 pill CTA(이메일).
- **footer**: 모노 소형 링크 한 줄.

## Do / Don't (안티-AI 체크리스트)

### Do
- 문서형 단일 흐름, 넉넉한 여백, 헤어라인으로 구조 표현
- 모노크롬 + 액센트 1개, 시스템 폰트
- 사실 기반 카피(마케팅 슬로건보다 구체)
- 코드 블록을 콘텐츠로 활용

### Don't
- "아이브로우 라벨 → 큰 슬로건 → 버튼 2개" 히어로 골격
- 기술스택 알록달록 칩 클라우드, ◆ 구분자, 기술 마키
- 그라데이션 배경·드롭섀도·글래스모피즘 떡칠
- 보라색 그라데이션, 이모지 불릿, Inter 떡칠
